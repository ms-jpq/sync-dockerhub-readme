---
name: Sync Readme

on:
  push:
    branches:
      - whale
  schedule:
    - cron: "0 0 * * *" # daily


jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Sync
        uses: ms-jpq/dockerhub-readme@a8cbec0558eb2e4cc898c8780e762dd9ac1e31b8
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: ${{ secrets.DOCKER_USERNAME }}/dockerhub-readme
          readme: "./README.md"

